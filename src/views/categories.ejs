<%- include('partials/header') %>
    <main>
        <h1>
            <%= title %>
        </h1>
        <ul>
            <% categories.forEach(category => { %>
                <li><%= category.category %></li>
            <% });%>
        </ul>
    </main>
    <%- include('partials/footer') %>